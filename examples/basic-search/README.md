# Basic Search Example

Demonstrates how to call the search endpoint from Python.

## Prerequisites

- arxiv-sentinel API running on `localhost:8000`
- At least one paper indexed

## Run

```bash
pip install httpx
python search.py
```
